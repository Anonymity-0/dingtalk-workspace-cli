---
category: Fixed
---

- **Markdown routing and diff contracts** — makes `markdown create --folder`
  detect the Drive or Doc destination before upload, publishes executable
  `markdown diff` parameter constraints, and improves mono/multi Agent routing.
